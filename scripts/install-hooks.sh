#!/usr/bin/env bash
# Run this to install git hooks for minecraft-mcp-rs
#
# 当前为占位实现：git hooks 安装功能尚未实现。后续会添加 pre-commit
# (cargo fmt / clippy / test) 与 commit-msg (Conventional Commits) 钩子。
# 在那之前，本脚本不做任何 git config 副作用，保持 `core.hooksPath` 未设置。
