# minecraft-mcp-rs — MCP server that controls a Minecraft bot

An MCP (Model Context Protocol) server backed by an actual Minecraft bot via the azalea library. Exposes bot capabilities (movement, block manipulation, inventory, combat, chat) as MCP tools consumable by LLM clients. Ships with an egui desktop UI for status and settings.

- **Stack:** Rust nightly (edition 2024; azalea 0.15.1's build script requires nightly), azalea 0.15.1 (Minecraft 1.21.11 bot — the ONLY supported Minecraft version), rmcp (MCP server), egui/eframe 0.34.3 (desktop UI), tokio 1.50 (async runtime), bevy_ecs 0.18 (azalea's ECS), schemars 1.0 (MCP JSON schemas), proptest (property testing)
- **Entry point:** `src/main.rs` — creates shared state + channel, spawns MCP server on background thread + egui on main thread. Bot connection is spawned on a dedicated OS thread from the UI (azalea's `ClientBuilder::start` internally creates a `LocalSet` which is `!Send`, preventing `tokio::spawn` on a multi-threaded runtime).
- **Library crate root:** `src/lib.rs`

## Commands

| Action | Command |
|--------|---------|
| Build | `cargo build` |
| Run | `cargo run` |
| Test (unit) | `cargo test --lib` |
| Test (all) | `cargo test` |
| Proptest | `cargo test --test proptest` |
| Integration | `cargo test --test integration` |
| Lint | `cargo clippy --all-targets` |
| Doc | `cargo doc --no-deps` |

Requires Rust nightly (edition 2024; `rust-toolchain.toml` pins a nightly **date** — azalea 0.15.1's build script enforces nightly, and a bare `nightly` let a fresh upstream compiler break azalea-core in CI on 2026-08-22, E0284 in `FixedBitSet`). Bumping the pinned date is part of dependency-upgrade work: change `channel`, then verify azalea builds and the gate suite passes. Dev profile uses `opt-level = 1` (with `opt-level = 3` for dependencies) for faster iteration.

## Development workflow

**默认流程：任何修改都在新分支上进行，提交 Pull Request，经用户审阅通过后合并。分支模型（详见 `CONTRIBUTING.md`）：`develop`（集成）→ `release`（预发布通道，push 即自动预发布）→ `master`（稳定版，tag 即稳定发布）。**

1. **建分支** — 功能/修复/文档分支从 `develop`（或已同步的 `master`）创建，命名建议：`feat/<slug>`、`fix/<slug>`、`docs/<slug>`。禁止直接向 `master` / `release` 提交；**禁止使用 `release/<X.Y.Z>` 分支名**（git refs 命名空间与 `release` 分支冲突，按版本准备分支改用 `hotfix/<X.Y.Z>` 等前缀）。
2. **开发** — 遵循下方「Conventions」与「规范」：`cargo fmt` → `cargo test`（全过）→ `cargo clippy --all-targets`（零警告）→ `cargo doc --no-deps`（零警告，`[lints.rustdoc]` deny 基线）→ 更新 `README.md` / `CHANGELOG.md` / `AGENTS.md`。
3. **提交** — 按 S-16 的原子提交拆分（CI → AGENTS.md → Cargo+npm → markdown 引脚/表 → CHANGELOG），每条提交只做一件事。
4. **PR** — `git push -u origin <branch>` 后用 `gh pr create` 提交 PR（默认目标 `develop`），PR 描述写明改动内容、验证结果（`cargo test` / `clippy` 输出）、影响面（含 wire 破坏性变更）。**必须等待用户审阅。**
5. **合并** — 用户审阅（可能要求修改）通过后合入 `develop`。
6. **预发布**（`release` 分支）— `develop` → `release` 的 PR 合并后，在 `release` 上把版本改为 `X.Y.Z-rc.N`、跑 `node npm/scripts/sync-versions.mjs`、补 CHANGELOG `[X.Y.Z-rc.N]` 节并 push：`release.yml` 自动发布**预发布版**（GitHub prerelease + npm `next`）。
7. **稳定发布**（`master`）— `release` → `master` 的发布 PR（版本定稿 `X.Y.Z`、更新 markdown 引脚/兼容表、CHANGELOG 定稿）合并后，在 `master` 打 `vX.Y.Z` tag：自动发布**稳定版**（GitHub Latest Release + npm `latest`）。随后 `master` 快进合并回 `develop`。

例外：用户在本会话中**明确指示**直接提交/直接发布时，可跳过分支+PR（例如一键发布指令）；否则一律走分支+PR。

## Architecture

Layered: **types → logic → state → bot → mcp → ui**, all in one crate.

```
src/
  types.rs            — Shared data types (BlockPos, BotCommand, WorldSnapshot, etc.)
  error.rs            — BotError enum (actionable error variants for AI agents);
                        re-exports BlockPos/ToolType/MaterialTier from types.rs
  config.rs           — AppConfig (UI settings) + RunStats (atomic counters)
  state.rs            — SharedState (thread-safe hub: ArcSwap for snapshot, RwLock for config,
                        AtomicBool for online/connecting/disconnect flags)
  block_data.rs       — Block/tool/material tables, best-tool selection
  mining_calc.rs      — Mining time formulas (the single canonical calculate_mine_time)
  command_validate.rs — Coordinate validation and command pre-checks
  snapshot.rs         — World snapshot data structures and chunk summaries
  tool_select.rs      — Best-tool selection logic
  utils.rs            — Common utility helpers (e.g. `to_snake_case`,
                        `contains_ascii_case_insensitive`)
  compound_ops.rs     — Compound operations (e.g. mine-and-collect pipeline)
  channel.rs          — Cross-thread BotCommand channel (tokio mpsc + oneshot);
                        ReceiverSlot + ReceiverLease for executor lifecycle
  i18n/               — UI internationalization (functional, one file per
                        language; lives at crate root so both ui/ and mcp/ can
                        use it without an mcp→ui dependency)
    mod.rs            — Language enum, TextKey enum, thread-safe current()/set()/tr()
    en.rs             — English lookup table
    zh_cn.rs          — Simplified Chinese lookup table
  logging.rs          — tracing-subscriber setup (stderr only; stdout = MCP transport)
  bot/                — Minecraft bot lifecycle
    mod.rs            — Re-exports
    connection.rs     — ConnectionManager (connect, reconnect with backoff, disconnect)
    events.rs         — azalea event handlers (player position, chunk loads, chat)
    commands.rs       — BotCommand → azalea action execution
    ops.rs            — Higher-level bot operations (move, mine, place, etc.)
    snapshot_updater.rs — Periodically snapshots world state into SharedState
  mcp/                — MCP server
    server.rs         — McpBotServer (rmcp ServerHandler), stdio or HTTP transport
    tools_*.rs        — Tool definitions organized by domain: query, movement, block,
                        item, container, combat, chat, plus unified `act`
    render.rs         — Top-down PNG world rendering for `get_world_view`
  ui/                 — Desktop UI
    app.rs            — egui app shell
    fonts.rs          — CJK system font auto-detection + injection into egui FontDefinitions
    settings.rs       — Settings panel (includes MCP transport / port / token / language)
    status.rs         — Status panel with live stats
    mcp_config.rs     — Copyable, live-generated MCP client JSON config
tests/
  integration.rs      — Mock-based end-to-end tests (no real MC server)
  proptest.rs         — Property-based tests for block_data, command_validate
```

## Conventions

- **Doc comments (`//!`)** on every module, doc comments (`///`) on every public type and function.
- **Section separators:** `// ═══`, `// ----`, or `// ≡≡≡` lines between logical sections within a file.
- **Error handling:** `BotError` (thiserror) for all bot/MCP errors; `eyre` for rarely-needed error context (no `anyhow` dependency).
- **Thread safety:** `Arc<SharedState>` shared across threads; `ArcSwap` for lock-free snapshot reads; `RwLock` for config/stats; `AtomicBool` for online flag; `Mutex` for container handle and chat messages.
- **Testing:** Unit tests in `#[cfg(test)] mod tests { .. }` at bottom of each source file; integration tests in `tests/`; property tests with `proptest` crate.
- **Logging:** `tracing` crate macros only; all output to stderr (`stdout` reserved for MCP JSON-RPC transport). Enabled via `init_logging()` called once at startup.
- **Naming:** Types PascalCase, enums PascalCase, functions/methods snake_case, module file names snake_case.
- **MCP tools:** Each tool module (tools_*.rs) exposes a builder function; tool parameters annotated with `#[derive(schemars::JsonSchema)]`.
- **Formatting:** No `.rustfmt.toml` — uses default `rustfmt`.
- **Rustdoc links:** `cargo doc --no-deps` must stay warning-free — `[lints.rustdoc]` denies broken/private intra-doc links and redundant explicit targets. Link private items as plain code spans (`` `item` ``), not `[]` links. The build.yml lint job enforces this plus `--no-fail-fast` tests and doctests; `audit.yml` runs `cargo audit` weekly over every committed lockfile (advisory-only for now).

## Notes

<!-- Quick-add space for future notes -->
- **H-1 mining tool equip (2026-08 audit-fix round):** `execute_mine_block`'s tool-selection state dispatches `MoveItemToHotbar` + `SwitchHotbarSlot` when the best tool sits only in the main inventory (slots 9-35) — `ToolSelection` gained `item_id`, and the state machine carries the `(target_slot, item_id)` pair so the follow-up `SwitchHotbarSlot` selects the slot the tool was moved into. The mining wait is therefore computed for the tool that actually ends up in hand (audit H-1: sleeps used the best tool's speed while the bot dug with its held item, so `wait_for_block_gone` always timed out → `MiningInterrupted` on every `act(Mine)` / default `break_block`). The ops.rs `MockBot` now enforces held-item mining speed: `mine_block` only removes the block when the held item matches `best_tool_for_block(&block_type)` (audit L-33). Regression: `test_mine_block_moves_main_inventory_tool_to_hotbar`.
- **H-2 place_block semantic (2026-08 audit-fix round):** `BotCommand::PlaceBlock(pos, …)` now means **the placed block occupies `pos`**. The executor right-clicks `pos − Up` (azalea's fabricated Up-face hit — the R-9 convention), pre-checks `y` in `-63..=320` (`y=-64` rejected: the clicked cell would sit below the world), that the click target is loaded, and that the effect cell is empty, auto-approaches via `find_standable_neighbor` when farther than 4.5 blocks, then verifies the placement by polling `wait_for_block_present`. The MCP layer rewrites the result message ("Placed stone at ...") only when `result.success` is true (audit M-7). Regressions: `test_place_block_verifies_effect_cell`, `test_place_block_clicks_cell_below`, `test_place_block_y_minus_64_rejected` (in `tools_block.rs::tests`).
- **M-10 restart-flag single ownership (2026-08 audit-fix round):** while a bot thread lives, the connect loop is the **sole** consumer of `take_config_restart` — including the TCP-connect-window cancel branch, which now routes through `cancel_branch_action` (checks the restart flag → `continue` instead of plain `break`, audit M-3). The headless supervisor consumes the flag only when no bot thread exists; `inner_wait_step` never touches it. `connect()` entry discards a stale flag via `prepare_connect_entry` (audit L-22 — an offline `update_settings` flag must not turn a later manual Disconnect into a reconnect). This kills the double-session race where supervisor + connect loop both consumed the flag and two azalea clients logged in with the same username, kicking each other. Regressions: `test_supervisor_does_not_consume_restart_while_thread_alive`, `test_tcp_window_cancel_with_config_restart_continues`, `test_connect_entry_discards_stale_config_restart`.
- **L-4 air-entry invariant (2026-08 audit-fix round):** production snapshots never contain air entries — the dirty-block single-read path now filters `is_air()` exactly like the dirty-chunk full scan, so the same world state yields the same snapshot shape on both paths. A broken block is **absent** from `block_index` (not present as `air`), which is what `execute_mine_block`'s Step-10 verification (`block_index.get(&pos).is_none()`) relies on. Regression: `test_dirty_block_update_to_air_removes_entry`.
- **L-23 monotonic stamps (2026-08 audit-fix round):** `last_command_at` / `mcp_activity_at` now store nanos since a process-start `Instant` anchor (`ACTIVITY_ANCHOR`) instead of wall-clock epoch milliseconds; the accessors return `Option<Instant>`, and `within_activity_window` / `is_headless_idle` are pure `Instant`/`Duration` decision helpers. An NTP step-back can no longer zero the `saturating_sub` and pin the bot "permanently active" (snapshot stuck at the fast 500 ms interval) or wedge the headless idle watchdog. Regressions: `test_activity_probes_monotonic`, `test_is_headless_idle_clock_anomaly_safe`.
- **M-5 movement margins (2026-08 audit-fix round):** `walk_direction` now routes its `goto` through `goto_with_margin` (it previously raced the command envelope); `collect_items` splits `command_timeout − MOVEMENT_REPLY_MARGIN` across targets with a 2 s per-target floor, and reports honest partial results (`visited` count + position) when the budget runs out instead of a bare `CommandTimeout` that discarded the visited count. Regressions: `test_walk_direction_uses_margin_wrapper`, `test_collect_items_partial_visits_reported`.
- **M-1 goto_with_deadline (2026-08 audit-fix round):** `BotActions::goto_with_deadline` (default implementation delegates to `goto`); `RealBotClient` shares `goto_inner`, and `goto_with_margin_with_timeout` passes its timeout through as a deadline so `fly_timeout_secs` bounds the pathfinder itself (previously the inner `goto` used the 30 s command envelope, so `fly_to` flights > 30 s failed with "pathfinding timed out after 30s"). Regression: `test_fly_to_inner_goto_honours_fly_timeout`.
- **`execute_command` rejection scan (2026-08 review round):** `handle_execute_command` uses `rejection_feedback_after` — it scans **every** System message after the baseline (not just the newest via `server_feedback_after`) and returns the newest one matching `is_command_rejection`. This matters because Minecraft reports a rejection as TWO messages (error title with the keyword + command echo with `<--[HERE]`, which carries no keyword and lands last). `server_feedback_after` remains for the success path (attach newest feedback). Regression: `test_execute_command_rejection_detected_two_line_feedback`. **Round-2 fix: the baseline is now a monotonic chat-message cursor (`SharedState::chat_cursor()`), not the deque length.** Chat messages carry monotonic sequence numbers (`chat_messages: Mutex<VecDeque<(u64, String, String)>>`, allocated under the lock; `chat_messages_since(cursor)` returns `seq >= cursor`). The old length-based baseline degenerated once the deque hit its cap (`MAX_CHAT_MESSAGES = 50`): `skip(50)` skipped every new message, so rejected commands were reported as successes in real sessions (unit tests with an empty deque never caught it). Regressions: `test_execute_command_rejection_detected_full_chat_queue`, `test_execute_command_success_feedback_full_chat_queue`, `test_chat_message_seqs_monotonic_across_eviction`. `get_chat_messages()` still returns `(sender, message)` pairs — the seq is internal. **Round-3: the rejection patterns now include `unknown item` / `no such item`** (vanilla's `/give` rejection is `Unknown item '...'` + the keywordless `<--[HERE]` echo); regressions `test_execute_command_rejection_detected_unknown_item`, `test_is_command_rejection_matches_unknown_item_variants`. The `get_chat_history` tool / UI labels document the 50-message cap (they used to say 10).
- **`give_item` commands-availability gate (2026-08 review round):** the gate re-probes live via `probe_commands_enabled` (extracted from `get_server_info` into `tools_query.rs` — the single source of truth) and rejects ONLY when the probe confirms `false`; it never trusts the snapshot's `commands_enabled` (which can be a stale `PermissionLevel` heuristic right after a reconnect). `handle_disconnect` clears the probe (`set_commands_probe(None)`) so it never crosses sessions. `probe_commands_enabled` semantics: `Ok` → `Some(true)`, `CommandRejected` → `Some(false)`, other errors keep the previous cached value. **Round-3: a rejection of the initial `/give` (e.g. unknown item id) is propagated as `CommandRejected` — never a fake `Gave N x ...` success.** Regression: `test_give_item_inventory_command_rejected_propagates`. **2026-08 audit-fix round:** the probe is bounded to 3 s via `send_command_with_timeout` (audit L-18 — a busy executor could previously stall `give_item` / `get_server_info(refresh)` for the full command timeout); `give_item` now propagates the executor's `BotResult` — a `/give` the server accepts but does not honour reports `success:false` instead of "Gave N x ..." (audit L-6); the swap fallback strips the `minecraft:` namespace prefix before matching the inventory (audit L-7, `minecraft:water_bucket` now matches `water_bucket`).
- **`act` result uses the live position (2026-08 review round):** `handle_act` reads `BotActions::position()` (new trait method — `RealBotClient` reads `azalea::entity::Position` directly; zero-wait) after the action and uses it as the nearby-filter centre AND overrides `self_info.position` / `position_precise` (via `Arc::make_mut`). Falls back to the snapshot when `None`. The throttled snapshot can lag a just-finished move by one interval (5 s when idle), which LLM clients misread as "did not arrive". Mock `MockBotClient` carries `live_position: Mutex<Option<[f64;3]>>` (default None); the ops.rs `MockBot` returns the snapshot position (kept in lock-step). **Round-2: `perception_radius=0` now returns EMPTY nearby blocks/entities** — the `<= 0` filter previously kept entities/blocks sharing the player's own cell, contradicting the tool contract "0 returns no nearby context at all".
- **`teleport` / FlyTo vertical leg via `/tp` (2026-08 round-2):** `BotActions::teleport` was REMOVED — mutating the local ECS `Position` component reported success but the server re-syncs the authoritative position every tick, so the bot never moved. `handle_teleport` (now `async`) sends `/tp x y z` and verifies the server reply via the chat cursor + `rejection_feedback_after` / `server_feedback_after` (same mechanism as `handle_execute_command`): no OP → `CommandRejected`; success attaches "Teleported ..." feedback. `handle_fly_to`'s vertical landing leg uses the same `teleport_via_command` helper instead of the local mutation. MCP `teleport` keeps the creative gate; `/tp` still needs OP on a vanilla server. Tests: `test_teleport` (asserts `/tp 50 70 100` chat), `test_teleport_rejected_without_op`, `test_fly_to_vertical_reaches_target` (asserts `/tp 10 70 0`).
- **`get_world_view` annotation counts are view-scoped (2026-08 round-2):** `render_topdown_enhanced` returns `(png, visible_block_columns, visible_entities)` — counted in the same loop that renders (block columns = distinct `column_best` slots painted, stacked blocks collapse to one; entities = in-radius count). The old `snapshot.blocks.len()` / `snapshot.entities.len()` reported the whole snapshot (230k blocks for a 9-block viewport). `WorldViewCache` gained `block_count`/`entity_count` so a cache hit returns the same numbers. Tests: `test_render_enhanced_counts_visible_columns_and_entities`, `test_get_world_view_annotation_counts_are_view_scoped`, `test_get_world_view_cache_hit_preserves_view_counts`.
- **`place_block` message names the block (2026-08 round-2):** the executor only knows the hotbar slot ("slot:N" encoding) so its message read "Placed 3 at ..." — opaque to the LLM. `tools_block::handle_place_block` (which has snapshot access) resolves `inventory.find(slot_index == item_slot)` → `item_id` and rewrites the message to "Placed stone at ..."; an empty slot is reported honestly as "(empty slot)". Executor / `BotCommand::PlaceBlock` contract unchanged.
- **Yaw normalization (2026-08 review round):** `utils::normalize_yaw` folds any radian angle into Minecraft's `[-180, 180)` degrees (`rem_euclid(360)` then fold ≥180). Applied at the **single write point** — `SelfPlayer::yaw` population in `snapshot_updater.rs` — so `get_bot_status` and the `get_world_view` annotation never expose unbounded accumulated turns (observed `-767.1°`). The renderer's sin/cos arrow is unaffected (periodic).
- **Fluid bucket limitation (2026-08 review round):** azalea 0.15.1's `block_interact` fabricates the `BlockHitResult` (`location: block center`, `direction: Up`) which the vanilla server rejects for bucket `UseItemOn` (but accepts for block placement / flint-and-steel). No upstream API exists to customize the hit. `handle_use_item_on_block`'s verification-timeout branch therefore returns `success:false` with `reason: "bucket_placement_unsupported"` + `/setblock`/`/fill` alternatives for non-milk `_bucket` items (see `is_fluid_bucket_item`); the generic "likely rejected" message is kept for other placement items. Do NOT "fix" this by pre-rejecting all buckets — cauldron-filling etc. may still land (effect-cell verification decides).
- **Stdio exit robustness (2026-08 review round):** headless `--stdio` no longer depends solely on stdin EOF (which may never arrive on Windows — inherited console handles have no EOF; pipe EOF needs every write end closed). Three layers: (1) `serve_stdio` races `tokio::signal::ctrl_c()` (reuses `shutdown_signal` from the HTTP path); (2) the rmcp patch (`patches/rmcp/src/service.rs`) adds `QuitReason::TransportWriteError` — `response_send_tasks` is now a `JoinSet<Result<(), T::Error>>` whose join arm breaks the serve loop when a response write fails (EPIPE = client gone), skipping the post-loop drain for that reason; (3) headless-only idle watchdog — `serve_stdio(..., headless: bool)` races `headless_idle_watchdog` which resolves after `HEADLESS_IDLE_TIMEOUT` (600 s) with no activity (pure gate `is_headless_idle(now, last_activity, started_at, timeout)` unit-tested). `main.rs` passes `headless` through. Patching the vendored rmcp is a documented dependency change (see `patches/` note). **2026-08 round-2: the watchdog's activity probe is MCP-REQUEST activity (`SharedState::mcp_activity_at_ms`, stamped at the entry of `initialize` / `ping` / `list_tools` / `call_tool`), NOT bot-command activity.** ZCode spawns per-session probe connections that send initialize/list_tools but never dispatch a bot command; keying on command activity killed those healthy sessions after 600 s ("MCP server connection closed unexpectedly"). The four `ServerHandler` overrides in `src/mcp/server.rs` delegate to the same logic the `#[tool_handler]` macro would generate (the macro's `has_method` guard skips generating overrides that already exist) and are the only hook point that doesn't require patching rmcp. `last_command_at` remains the snapshot-relaxation probe (semantically independent — do not merge the two).
- **`execute_command` feedback detection (1.1.5):** `handle_execute_command` is now async: it records the chat baseline, sends the command, waits `COMMAND_FEEDBACK_WAIT_MS` (400 ms), then diffs new **System** messages via `server_feedback_after` and matches them against the `is_command_rejection` patterns (`Incorrect argument for command` / `Unknown or incomplete command` / `Unknown command` / `You do not have permission to use this command` / `You are not allowed to use this command` / `Cannot execute`). A hit returns `BotError::CommandRejected { command, feedback }` (-32009, `reason: command_rejected`, retryable); otherwise success is reported with the newest System feedback attached to the message/data. Player messages are never treated as server feedback. **2026-08 audit-fix round:** `execute_command` trims the input once so the empty-check and the `/`-prepend use the same string (audit L-5 — `" gamemode creative"` used to become the unknown command `"/ gamemode creative"`); the chat tools (`send_chat` / `execute_command` / `set_game_mode`) now return the serialized `BotResult` JSON object like every other action tool (audit L-12).
- **Snapshot force-refresh (1.1.5):** `get_self_info(force=true)` / `get_inventory(force=true)` / `get_server_info` / `collect_items` request an immediate snapshot rebuild via `SharedState::request_snapshot_refresh() -> oneshot::Receiver<()>` (single-slot: a new request drops the previous one). `handle_tick` polls `take_snapshot_force_requester()` **before** the throttle gate; when present it skips the interval check and the build task signals the receiver. `refresh_snapshot_and_wait` (`tools_query.rs`) bounds the wait at 3 s and falls back to reading the current snapshot. The `force` params default to `true` (serde `default_true`) so existing callers get fresh reads.
- **`commands_enabled` probe merge (1.1.5):** `get_server_info` sends `BotCommand::ExecuteCommand("/seed")` (3 s bounded via the command channel) when `refresh=true` or no probe result is cached: `Ok` → `set_commands_probe(Some(true))`, `CommandRejected` → `Some(false)`, other errors keep the previous value. `build_snapshot_inner` resolves `commands_enabled` via `resolve_commands_enabled` = probe result `.or_else(PermissionLevel heuristic)` so snapshot builds merge the probe. Response includes `bot_busy` (`SharedState::executor_busy`, set by `run_with_lease` around each dispatch).
- **Movement timeout partial results (1.1.5):** `handle_move_to` / `handle_smart_move` / `handle_fly_to` route `goto` through `goto_with_margin`, which wraps `BotActions::goto` in `tokio::time::timeout(timeout - MOVEMENT_REPLY_MARGIN(1s))` so the executor replies **before** `send_command`'s envelope timeout fires. On timeout it calls `stop_pathfinding()` (new `BotActions` method) and returns a `BotResult` with `{reason:"timeout", position, start, target, distance_moved, distance_xyz, timeout_secs}` instead of a bare `CommandTimeout`. `RealBotClient::stop_pathfinding` = `client.stop_pathfinding()`.
- **`drop_item` verification (1.1.5):** `handle_drop_item` reads `inventory_entries()` before/after the throw-click (250 ms wait): `removed >= count` → success with `{verified:true}`; empty-after-nonempty-before → success with `container_window_open:true` caveat; slot unchanged → `success:false` (empty slot vs server-rejected click are distinguished in the message). The mock `drop_item` now mutates the inventory so the branches are unit-testable.
- **`set_hotbar_item` / `MoveItemToHotbar` (1.1.5):** `BotCommand::MoveItemToHotbar(hotbar_slot, item_id, count)` finds the first inventory slot with `item_id` and count ≥ requested, maps the logical index to a player-menu slot (hotbar 0-8 → 36+slot, main 9-35 → identity) and issues `SwapClick { source_slot, target_slot }` via the new `BotActions::swap_hotbar` (`RealBotClient` guards on `get_inventory().id() == 0` — a container window makes the swap target the wrong menu). The handler verifies the hotbar slot afterwards; missing item → `InvalidParams`. Cannot conjure items.
- **Corpse filter (1.1.5):** `collect_entities` skips entities whose `Health` component is `Some(h)` with `h <= 0.0` — a killed mob lingers in the ECS until the server's RemoveEntities/despawn cleanup, so it previously showed up with `health: 0.0`. Item entities (no Health) are unaffected. Test: `test_collect_entities_skips_dead_entities`.
- **`ObstacleInfo` (1.1.5):** `smart_move`'s `obstacle` payload is now `{block_type, x, y, z}` (serialized via `obstacle_to_json`) instead of a bare string — a breaking wire change. `find_obstacle_block` scans the interpolated XZ line across three Y layers (y, y+1, y-1) then falls back to a 3×3×3 neighbourhood around `current`; `None` only when no solid block is found anywhere.
- **Run-stats accounting (1.1.4):** `CommandExecutor::dispatch` is a thin wrapper around `dispatch_inner` that increments `RunStats` (`commands_processed` + `commands_succeeded`/`commands_failed`) on **every** command reaching the executor — including compound-op sub-commands and validation/offline rejections (the UI panel reports "received commands"). The atomics use `Ordering::Relaxed`. Regression tests: `test_dispatch_records_success_in_run_stats` / `test_dispatch_records_failure_in_run_stats` in `bot/commands.rs::tests`.
- **HTTP shutdown (1.1.4):** `serve_http`'s graceful shutdown races the `shutdown_token` against `tokio::signal::ctrl_c()` via `shutdown_signal` (`src/mcp/server.rs`). Ctrl+C now exits cleanly in every mode — in particular headless mode with the default HTTP transport, which previously deadlocked (the token was only triggered after `serve_http` returned, and `serve_http` only returned after the token fired). UI/stdio paths are unchanged. Test: `test_shutdown_signal_returns_on_token_cancel`.
- **`update_from_tick` → `build_and_store` (1.1.4):** `SnapshotUpdater::update_from_tick` (which cloned the whole snapshot just to return it) was replaced by `check_and_update_timer()` (pure throttle check, no reset side effects for callers) + `build_and_store(&bot) -> bool` (moves the snapshot into `SharedState`, returns whether a repaint is due). `handle_tick` runs the throttle check **before** spawning the build task, so throttled ticks (~18 of 20 per second) no longer spawn a wasted task.
- **`SnapshotBuilder` production constructor (1.1.4):** `SnapshotBuilder::new(old_blocks: Vec<BlockEntry>)` is the per-tick path — it carries over **only the block list** (not the old snapshot, whose `block_index` HashMap and unused fields used to be deep-cloned every 500 ms). Unset fields fall back to `WorldSnapshot::default()`. `SnapshotBuilder::from_old(old: WorldSnapshot)` keeps the old full-snapshot semantics for tests. The per-tick caller must supply every field via `with_*` (it does).
- **Block-name cache (1.1.4):** `block_state_to_name` resolves each distinct `BlockState` id once into a `LazyLock<Mutex<HashMap<BlockState, String>>>` cache instead of paying `format!` + `to_snake_case` per block per tick. `BlockState` is a `Copy` `Hash` u16 wrapper.
- **`contains_ascii_case_insensitive` (1.1.4):** `src/utils.rs` hosts the non-allocating ASCII substring matcher; `get_nearby_blocks` uses it per block instead of `block_type.to_lowercase()` (which allocated per block).
- **`AttackEntity` snapshot guard (1.1.4):** `handle_attack_entity` rejects a target missing from the world snapshot with `BotError::InvalidParams` before attacking (the MCP layer already enforced the same check). Tests: `test_attack_entity_missing_from_snapshot_rejected`.
- **规范:** 函数式编程，大量注释。写完后使用`cargo fmt`格式化；及时编写`cargo test`自动化测试，`cargo test`全过才能交付，编写遵循TDD；需要运行`cargo clippy`检验，全过才能交付；最后更新`README.md`、`CHANGELOG.md`和`AGENTS.md`。**提交走「Development workflow」：默认在新分支上修改，提交 PR 并经用户审阅合并，禁止直接推 `master`；用户明确指示直接提交/发布时除外。**
- **Settings panel:** Uses `EditConfig` local edit buffers (in `app.rs`); fields rendered via `TextEdit::singleline`/`DragValue`. Edits applied to `SharedState` only on Connect. The `sender` parameter was removed from `settings_panel` — the UI doesn't send commands directly.
- **MCP Config panel:** Renders a copyable JSON config (with the executable's absolute path resolved at runtime) for MCP clients like Claude Desktop / Cursor. Uses egui 0.34.3's clipboard API; schemars 1.0 `Schema` (via `schema_for!`) drives any schema rendering.
- **Bot connection:** Spawned on dedicated OS thread (not `tokio::spawn`) because `ConnectionManager::connect()` contains `LocalSet` which is `!Send`. The thread's `JoinHandle` is held by `MinecraftApp` and joined on `Drop` for clean exit. `Drop::join` has a 3-second timeout so closing the window never hangs.
- **Command executor:** Wired into `Event::Spawn` via `spawn_local`. The command receiver is stored in a `ReceiverSlot` (`Arc<Mutex<Option<BotCommandReceiver>>>`) and leased out via `ReceiverLease` on Spawn; when the executor is aborted on `Event::Disconnect`, the lease drops and returns the receiver to the slot for the next reconnect. `Event::Disconnect` also writes `AppExit::Success` to the ECS (`bot.ecs.lock().write_message(AppExit::Success)`) so `ClientBuilder::start` returns and the connect loop can retry (azalea 0.15.1 removed `Client::exit()`).
- **Connect/Disconnect:** `SharedState` has `bot_connecting` (AtomicBool) and `disconnect_requested` (AtomicBool) flags. `try_begin_connecting` guards against double-spawn. `request_disconnect` tells the reconnect loop to stop; the Disconnect button sets it. `clear_connecting` is called when the connect loop exits. A `CancellationToken` (from `tokio-util`) is stored in `SharedState` and cancelled on disconnect so the reconnect backoff sleep returns immediately instead of blocking shutdown. **`ClientBuilder::start()` is wrapped in `tokio::select!` with `cancel_token.cancelled()`** so a disconnect request during the TCP connect attempt (~5 s) aborts immediately via `break` instead of waiting for `start()` to return (variable name `start_cancel` to avoid shadowing the later `cancel_token` locals used for backoff/retry sleeps).
- **Console window on Windows:** `src/main.rs` no longer carries the crate-level `#![cfg_attr(not(debug_assertions), ...)]` attribute that used to hide the console on release builds — it was removed so **release builds keep the console window too**. The console carries `tracing` logs (stderr) and, in `--stdio` headless mode, the MCP JSON-RPC transport, so hiding it on Windows made headless/stdio diagnostics impossible. All builds now show the console on Windows.
- **Connection errors:** `SharedState::last_error` (behind a `Mutex<Option<String>>`) surfaces the most recent connection failure to the UI; the Status panel renders it in red. Connection failures are fail-fast — the reconnect loop stops retrying so the user sees the error and can manually retry, rather than looping infinitely.
- **Type unification:** `error.rs` re-exports `BlockPos`, `ToolType`, `MaterialTier` from `types.rs` — no duplicate definitions. `ToolType` has 7 variants (Pickaxe, Axe, Shovel, Hoe, Sword, Shears, Hand). No `to_error_*` bridge helpers needed.
- **Snapshot building:** `handle_tick` delegates to `SnapshotUpdater::update_from_tick` — the inline `build_and_update_snapshot` and helper functions were deleted from `events.rs` to avoid duplication with `snapshot_updater.rs`.
- **Mutex poisoning recovery**: Extended from `SharedState` to all shared mutexes (including `channel.rs` command receiver slot, `bot/events.rs` executor handle, and `logging.rs` test helpers). All use `.unwrap_or_else(|e| e.into_inner())` to prevent cascade crashes.
- **`SharedState::modify_snapshot` 约定:** 事件 handler 更新快照部分字段（entities / self_player.health 等）**必须**用 `SharedState::modify_snapshot<F: FnMut(&mut WorldSnapshot)>(&self, f: F)`，基于 `ArcSwap::rcu` 原子读-改-写。闭包签名为 `FnMut(&mut WorldSnapshot)`（**不是 `FnOnce`**，因为 `rcu` 在并发更新下可能重试）。**禁止**再用 `read_snapshot().clone()` + `update_snapshot()` 模式 —— 该模式会丢更新（`SnapshotUpdater` 在 await 点交错时会把字段原样写回，抹掉事件 handler 的修改）。`handle_death` / `add_player_to_snapshot` / `handle_remove_player` / `handle_update_player` 已迁移到此 API。
- **`command_sender` 注入链路约定:** `Act::Mine` 需要委托 `CompoundOpExecutor` 完整执行（选工具→走过去→挖掘→验证）。由于 azalea ECS 通过 `BotState::default()` 构造 `BotState`，无法直接传参，采用与 `INJECTED_SHARED_STATE` / `INJECTED_COMMAND_RECEIVER` 同模式的 `Mutex<Option<T>>` 注入：`ConnectionManager::connect(command_receiver, egui_ctx, command_sender)` 在重连 `loop` **内部**（C-2 修复）每次迭代 `events::INJECTED_COMMAND_SENDER.set(Some(command_sender.clone()))`；`BotState::default` 读取该 static 得到 `command_sender: Option<BotCommandSender>`；`handle_spawn` 在 `async move` 闭包**外**提前 `let command_sender = state.command_sender.clone();`（避免 E0521 borrowed data escapes）传入 `CommandExecutor::new_for_lease(client, shared_state, command_sender)`；`CommandExecutor::sender: Option<BotCommandSender>` 标注 `#[allow(dead_code)]` —— **C-1 修复后 `handle_act` 不再使用 `sender` 字段**，仅保留以免改动 `connect` 调用链；`handle_act(Mine)` 现在直接把 `self`（`&CommandExecutor`）传给 `CompoundOpExecutor::execute_mine_block(self, pos, true)`，`CompoundOpExecutor` 已重构为单元结构 + 关联函数（`fn execute_mine_block<B>(executor: &CommandExecutor<B>, pos, verify) -> ...`），通过 `executor.dispatch(cmd)` 直接派发子命令而非走 `BotCommandSender` 通道（旧实现会在单消费者执行器循环上自死锁）。递归调用链 `dispatch → handle_act → execute_mine_block → dispatch` 通过在 `handle_act` 内用 `Box::pin(execute_mine_block(...))` 包装解决 E0733（async fn 递归）。`INJECTED_COMMAND_SENDER` 仍按 C-2 在 `connection.rs::connect` 的 `loop` 内每轮重新 set（不再被 `CompoundOpExecutor` 消费，保留供未来用途）。`MinecraftApp` 持有 `sender: BotCommandSender` 字段（来自 `main.rs` 的 `sender_for_egui`），`connect_bot` clone 一份传入 `manager.connect(...)`。
- **`shutdown_token` 生命周期约定:** `SharedState` 持有 `shutdown_token: Mutex<CancellationToken>`（来自 `tokio-util`，与 `cancel_token` 字段独立 —— `cancel_token` 用于打断 reconnect backoff sleep，`shutdown_token` 用于 MCP graceful shutdown，两者生命周期独立）。通过 `shutdown_token() -> CancellationToken` 暴露（克隆返回）、`trigger_shutdown()` 触发。`MinecraftApp::drop` 调用 `trigger_shutdown()`；`serve_http` 在 `app` 构造前 `let shutdown_token = state.shutdown_token();`（因为 `.with_state(state)` 消费 state），再用 `axum::serve(listener, app).with_graceful_shutdown(async move { shutdown_token.cancelled().await; })`（**必须用 `async move` 包装** —— `CancellationToken::cancelled()` 返回的 future 借用 token，非 `'static`，不满足 `with_graceful_shutdown` 的 `Send + 'static` 约束）；`serve_stdio` 在 `McpBotServer::new(state, sender)` 前 `let shutdown_token = state.shutdown_token();`，再用 `tokio::select!` 并联 `running.waiting()` 与 `shutdown_token.cancelled()`，shutdown 触发时立即返回不等 stdin EOF。MCP 线程 `JoinHandle` 现存于 `MinecraftApp`，drop 时 join（3 秒超时，与 bot 线程一致）。
- **`tick_abort_handles` 约定:** `BotState` 新增字段 `tick_abort_handles: Arc<Mutex<Vec<AbortHandle>>>`，追踪 `handle_tick` 中 `spawn_local` 的 tick 任务 `AbortHandle`。`handle_disconnect` 时 abort 全部并清空，防止 ECS 拆除后 tick 任务调用 `bot.component()` / `bot.world()` 触发 panic 或写入陈旧快照。模式与 `executor_handle` 一致（`take()` + abort）。
- **Command timeout:** `BotCommandSender::with_timeout` honours `AppConfig::command_timeout_secs`; `main.rs` wires the configured value at startup.
- **UI i18n:** Functional, one-file-per-language module under `src/ui/i18n/`. `Language` enum (`En`, `ZhCn`) with `Default = En`; `TextKey` enum enumerates all UI strings; `tr(key) -> &'static str` is a pure dispatch to the current language's `lookup`. Current language is held in a `static RwLock<Language>` (`current()`/`set()`). `AppConfig::language` persists the choice (`#[serde(default)]` for backward compat). The Settings panel Language dropdown calls `i18n::set()` on change for next-frame effect (no reconnect). MCP tool descriptions and JSON field names stay English (external API contract).
- **CJK fonts:** `src/ui/fonts.rs::install_system_cjk_fonts(ctx)` probes the platform default CJK font (Windows `msyh.ttc`, macOS `PingFang.ttc`, Linux Noto/WenQuanYi) at startup and injects it into egui `FontDefinitions` (appended to `Proportional` and `Monospace` as a fallback — see R-3). Falls back to the default font with a `warn` log if none found — never panics. Called once in the eframe creation closure.
- **CI (build):** `.github/workflows/build.yml` matrix-builds the **dev** binary for Linux × x86_64/aarch64, macOS aarch64 only, and Windows × x86_64/aarch64 using native ARM runners (`ubuntu-24.04-arm`, `macos-latest`, `windows-11-arm`). Artifacts named `minecraft-mcp-rs-<os>-<arch>`. The `push` trigger is limited to `develop` — master is stable-only and its builds run through the release pipeline; the `pull_request` trigger is also scoped to PRs targeting `develop`, a top-level `concurrency` group with `cancel-in-progress: true` cancels stale runs, and the `lint` job runs in parallel with the build matrix (no `needs: build`). **CI split (compile-time budget):** the run-once checks (fmt → clippy → test) live on `develop` — a PR targeting develop runs lint ONLY (the multi-platform matrix build is skipped via `if: github.event_name != 'pull_request'`), and the develop push runs lint + the full dev matrix once per merged change; `.github/workflows/release.yml` performs ONLY `--release` builds + publish (no duplicate lint job, `release`/npm-publish `needs: [build, mode]`), so the release path never re-compiles the test target matrix.
- **Docs (VitePress):** `docs/` holds a bilingual (English + 简体中文) VitePress site. Config split mirrors vuejs/vitepress: `config/index.ts` assembles `locales` (root=EN, `zh`=ZH); `en.ts`/`zh.ts` hold per-locale nav/sidebar/theme text. `base` reads `BASE_PATH` env var (default `/minecraft-mcp-rs/`) for GitHub Pages project sites. `.github/workflows/deploy-docs.yml` builds with Node 24 and deploys via `actions/deploy-pages@v5`. Run locally: `npm install && npm run docs:dev`.
- **Dependency patches:** `patches/rmcp/` and `patches/rsa/` are tracked in git and required by the `[patch.crates-io]` section in `Cargo.toml`. Do not add them to `.gitignore`; CI and fresh clones need them on disk. When updating either patch, keep the upstream license files intact and document the change in `README.md` / `CHANGELOG.md`.
- **GitHub Actions runtime:** workflows under `.github/workflows/` use the latest Node.js LTS/runtime supported by GitHub-hosted runners. When GitHub deprecates an old Node runtime, upgrade official actions to their newest major version and bump `node-version` accordingly; document the change in `CHANGELOG.md`.
- **Runner maintenance:** when a GitHub-hosted runner image is deprecated, suffers from long queues, or is otherwise unmaintainable, drop the affected target or migrate it to the latest stable `-latest` label, then document the change in `CHANGELOG.md`.
- **Injection globals are `Mutex<Option<T>>`:** the four values injected into `BotState::default` (`INJECTED_SHARED_STATE`, `INJECTED_COMMAND_RECEIVER`, `INJECTED_EGUI_CTX`, `INJECTED_COMMAND_SENDER`) live in `Mutex<Option<_>>` rather than `OnceLock` so they can be reset on disconnect / between tests. **C-2 修复后：`ConnectionManager::connect` 在重连 `loop` 内部每次迭代都重新 `set` 这四个全局**（不再是在进入 azalea 循环前只 set 一次），因为 `handle_disconnect` 会把它们全部清空回 `None`，旧实现的"循环外只 set 一次"导致重连时所有注入字段为 `None`，bot 在 `Spawn` 时拿不到 `shared_state` / `command_receiver` / `egui_ctx` / `command_sender`。`command_sender` 每轮迭代 `.clone()` 一份。`handle_disconnect` 仍负责清空全部回 `None`。这保证重连和单元测试都能看到新鲜的注入状态。 **`ReceiverLease::take` 失败需 yield 重试（≤100ms）：** 在 `handle_spawn` 中，当 `Event::Spawn` 比 `ReceiverSlot` 的清空（`handle_disconnect` → `executor_handle.take()`）早到（例如上一连接刚断开、新连接立刻建立的快速重连场景），首次 `slot.lock().unwrap_or_else(...).take()` 可能返回 `None`。正确做法是循环 `tokio::task::yield_now().await` + `tokio::time::sleep(Duration::from_millis(100))` 重试 100ms（最多 ~10 次），如果仍然 `None` 则进入"无执行器" warn 路径（log + 继续接收 azalea 事件但不处理 MCP 命令）。禁止立刻 return/spawn 失败 — 那样 MCP 工具会在重连期间一直报"bot offline"。回归测试：`test_receiver_lease_take_retries_during_reconnect` 在 `src/bot/events.rs::tests` 中并发触发"disconnect → spawn"序列，断言重试期间 lease 成功拿到 receiver。
- **`BotCommand::UseItemWithSlot`:** use this single command when an MCP tool needs to "switch to slot X and then use the item". The switch and the use happen inside the same executor critical section, so concurrent commands cannot interleave a different `SwitchHotbarSlot` between them. `tools_item::handle_use_item` sends `UseItemWithSlot(slot)` when `item_slot` is provided, otherwise falls back to parameterless `UseItem`.
- **Nearby queries are parameterised:** `get_nearby_blocks` and `get_nearby_entities` accept a `radius` argument (and `get_nearby_blocks` also accepts an optional `filter_type` substring). The tool input structs derive `Deserialize` and `rmcp::schemars::JsonSchema` and are wired through `Parameters<T>` in `server.rs`; the handlers no longer hardcode a radius.
- **MCP tool schemas use derive:** input structs in `tools_*.rs` use `#[derive(Deserialize, rmcp::schemars::JsonSchema)]`. Hand-written `impl JsonSchema` is reserved for cases where the derive truly cannot express the schema; adding it just to work around an old `schemars`/`rmcp` conflict is not acceptable because it drifts silently when fields change.
- **Tool schema range annotations:** bounded numeric MCP parameters (e.g. `WalkDirectionInput.distance`, `SwitchHotbarSlotInput.slot`) carry `#[schemars(range(min = ..., max = ...))]` so clients see the limits in the generated JSON Schema.
- **Common `to_snake_case` helper:** `src/utils.rs::to_snake_case` is the single implementation for converting azalea registry variant names (e.g. `IronPickaxe`) into snake_case item ids. Both the command executor and snapshot updater import it from `crate::utils`; do not re-implement it locally.
- **Deprecated `block_data::find_best_tool_in_inventory`:** this function is `#[deprecated(note = "use tool_select::find_tool_in_inventory instead")]` and delegates to `tool_select::find_tool_in_inventory`. New code and tests should call the `tool_select` version directly; the deprecated re-export remains only for backward compatibility.
- **Block-update packets (M-2):** `Event::Packet(Arc<ClientboundGamePacket>)` is the catch-all for game packets — azalea 0.15.1 has no dedicated `BlockUpdate` event variant. `handle_event` matches `Event::Packet(packet)` and dispatches to `handle_packet_block_updates(state, &packet)`, which pattern-matches `ClientboundGamePacket::BlockUpdate(data)` → `dirty_tracker.mark_block_dirty(data.pos)` and `ClientboundGamePacket::SectionBlocksUpdate(data)` → iterates `data.states` and computes each block's world `BlockPos` via `data.section_pos + entry.pos` (azalea-core provides `impl Add<ChunkSectionBlockPos> for ChunkSectionPos`). All other packet variants are ignored. The tracker lock uses `.unwrap_or_else(|e| e.into_inner())` for poisoning recovery. Without this handler, single-block updates from the server (block break/place by other players, piston extension, water flow) would never mark the snapshot dirty and the bot would see stale world state.
- **`find_standable_neighbor` (M-4):** `execute_mine_block` must NOT use the target block's own position as the `MoveTo` destination — the bot would path into the block and get stuck. Instead, call `compound_ops::find_standable_neighbor(&snapshot, target_pos)` which scans the 8 horizontal neighbours × 3 Y levels (priority: same Y → y+1 → y-1) and returns the first air block with a solid block below it. Block lookups go through `WorldSnapshot::block_index` (M-12) — O(1) per check, not O(N) linear scan. If no standable neighbour exists, the state machine transitions to `Failed(PathfindingFailed { move_target: target_pos })` rather than attempting an unreachable path.
- **`WorldSnapshot::block_index` (M-12):** `WorldSnapshot` carries a `block_index: HashMap<BlockPos, usize>` (`#[serde(skip)]` — derived from `blocks`, not part of the MCP JSON contract). `SnapshotBuilder::build` constructs it via `blocks.iter().enumerate().map(|(i, b)| (b.position, i)).collect()`. Code that needs to look up a block by position MUST use `snapshot.block_index.get(&pos)` → `&snapshot.blocks[idx]` rather than `snapshot.blocks.iter().find(|b| b.position == pos)` — the index is O(1), the linear scan is O(n) and was a hotspot for large snapshots. New `WorldSnapshot` literals must add `..Default::default()` (or `block_index: HashMap::new()`) since the field is non-optional. **`execute_mine_block` MUST go through `block_index` in both Step 2 (look up the target block's `block_type` to choose a tool) and Step 10 (verify the block was actually broken by re-reading via `block_index.get(&pos).is_none()`).** Linear `Vec::iter().find()` lookups inside the state machine are forbidden — they make the hot path O(n) per tick and have repeatedly regressed this code path. Regression test: `test_mine_block_uses_block_index` with a 5000-block snapshot must complete in < 1 ms.
- **`McpServerStatus` (M-13):** `SharedState` exposes `mcp_server_status: Mutex<McpServerStatus>` with `set_mcp_server_status()` / `get_mcp_server_status()`. `serve_http` calls `set_mcp_server_status(Running(addr))` after a successful bind, `Failed(msg)` on bind failure (also calls `set_last_error`), and `Stopped` when the server exits. `serve_stdio` calls `Stdio` at start and `Stopped` at end. The Status panel renders the current value so users can see whether the MCP server is up, where it's listening, or why it failed — without checking the terminal.
- **First-connect retry (M-9):** `ConnectionManager::connect` retries the first connection (`!was_online`) up to `MAX_FIRST_CONNECT_RETRIES = 3` times with a 2-second interval before falling through to fail-fast. This handles transient DNS / network blips on startup without entering an infinite retry loop. The retry sleep races `cancel_token()` so a disconnect request aborts immediately. Subsequent reconnects (after the bot was online) remain fail-fast — only the very first connection gets the retry window.
- **HTTP TLS warning (M-8):** when `mcp_transport == McpTransport::Http` AND `mcp_address` is not in `["127.0.0.1", "::1", "localhost"]`, both the Settings panel (below the Bind Address input) and the MCP Config panel render a red warning: "⚠ No TLS — use trusted network or reverse proxy". This reminds users that MCP HTTP transport has no encryption and should be put behind a TLS-terminating reverse proxy (e.g. nginx) when binding to a non-loopback interface.
- **`Offline` vs `CommandTimeout` semantics (S-4):** `BotCommandSender::send_command` distinguishes two failure modes: (1) `BotError::Offline("bot command channel closed")` — the receiver was never there or has been dropped; (2) `BotError::Offline("bot command responder dropped without reply")` — the receiver accepted the command but dropped the oneshot responder without replying (executor aborted/panicked mid-command). The third mode, `BotError::CommandTimeout`, is reserved for the genuine "responder is alive but slow" case (the `tokio::time::timeout` branch). Previously the responder-dropped case was incorrectly classified as `CommandTimeout`, hiding the fact that the bot was gone rather than merely slow. Tests asserting "responder dropped → Offline" live in both `src/channel.rs::tests::test_offline_when_responder_dropped` and `tests/integration.rs::test_offline_returned_when_responder_dropped*`. **MCP error mapping (Offline → -32000 SERVICE_UNAVAILABLE):** when `BotError::Offline(_)` is converted to MCP `ErrorData` (via `From<BotError> for ErrorData` in `src/error.rs` AND the `IntoCallToolResult` impl), the resulting JSON-RPC code MUST be `-32000` (JSON-RPC "server error" range, semantically `SERVICE_UNAVAILABLE`) — NOT the generic `-32603 InternalError` it was previously mis-mapped to. The error data field carries `serde_json::json!({"reason": "bot_disconnected"})` so MCP clients can distinguish "bot is gone, retry later" from "input is invalid" (`-32602`) and from "internal server error" (`-32603`). Both `test_into_mcp_error_offline_uses_service_unavailable` (the new assertion) and the updated `test_into_mcp_error_offline` (asserting the `reason` payload) live in `src/error.rs::tests`.
- **Lazy command logging (S-5):** `send_command` clones `cmd` into `cmd_for_log` and uses `tracing`'s `?cmd_for_log` lazy formatting (`trace!(command = ?cmd_for_log, ...)`) so the `Debug` formatting is only paid when the log level is enabled. The previous `format!("{:?}", cmd)` ran unconditionally on every call. The `format!` is now only in the `CommandTimeout` error path (which needs an owned `String` for the error variant).
- **Default log filter (S-7):** `logging.rs::init_logging` uses `EnvFilter::try_from_default_env().unwrap_or_else(|_| EnvFilter::new("minecraft_mcp_rs=debug,azalea=warn"))`. The default `minecraft_mcp_rs=debug` makes bot internals visible without setting `RUST_LOG`, while `azalea=warn` suppresses azalea's chatty info logs. Override with `RUST_LOG=info` or any other filter as usual.
- **Harvest Level (H-1):** vanilla Minecraft's tool tier system is enforced. `block_data::HARVEST_LEVEL: HashMap<&str, u8>` maps a block's required harvest tier: `Wood=0, Stone=1, Iron=2, Diamond=3, Netherite=4` (lower numbers are weaker). `tool_select::find_tool_in_inventory` now takes a `required_harvest_level: u8` parameter; any tool whose own `ToolMaterialTier < required` is rejected as `None` (NOT optimistically selected). The table covers at minimum `Stone` (→Stone, 1), `Cobblestone` (→Stone, 1), `IronOre`/`DeepslateIronOre` (→Stone, 1), `DiamondOre`/`DeepslateDiamondOre` (→Iron, 2 — needs iron pick to harvest the ore block, then drops diamond; some sources say stone is enough to break but no drops, iron guarantees the drop), `Obsidian` (→Diamond, 3), `AncientDebris` (→Diamond, 3), and `NetheriteBlock` (→Netherite, 4). When `find_tool_in_inventory` returns `None`, the caller MUST surface a `BotError::ToolNotFound { alternatives: [<next-tier tool name>] }` (e.g. "use an Iron Pickaxe to mine Diamond Ore") so the LLM client can tell the user to upgrade. **木镐不能挖钻石矿** (wood pickaxe cannot mine diamond ore — the best tool is silently downgraded, which loses resources and the player does not know why). Regression tests: `test_harvest_level_wood_cant_mine_diamond` (asserts `None` + `alternatives: ["IronPickaxe"]`) and `test_harvest_level_diamond_mine_diamond_ore` (asserts `Some(DiamondPickaxe)`). `mining_calc::calculate_mine_time` is unaffected — harvest level is a gate, not a speed multiplier. **2026-08 audit-fix correction (L-3/H-3):** `Stone` / `Cobblestone` are deliberately listed at **level 1 (Stone+)** — a conservative deviation from vanilla, where ANY pickaxe (level 0) can break them; `test_harvest_level_known_blocks` pins `Stone=1`, `Cobblestone=1`. The same documented conservative over-requirement applies to `coal_ore`, `netherrack`, `end_stone`, `purpur_block`/`purpur_pillar`, `deepslate`, and `nether_quartz_ore` (level 1 vs vanilla 0 — only a wooden pickaxe is affected). `nether_gold_ore` was corrected from level 1 to **level 2 (iron+)**: vanilla 1.21 requires an iron pickaxe for drops, and stone picks previously mined it with no drops while still reporting success (audit H-3; regression `test_harvest_level_nether_gold_ore_requires_iron`).
- **`ClearGuard` for `bot_connecting` (R-1):** `src/ui/app.rs` defines `ClearGuard<'a>(&'a SharedState)` whose `Drop` calls `state.clear_connecting()`. The bot connection thread wraps its body with `let _clear_guard = ClearGuard(&state);` as the first statement after `spawn`. This RAII guard ensures that **even if the thread panics** (unwinding calls `Drop`), the `bot_connecting` flag is cleared. Without this, a panic in `Runtime::new()` or any other early failure before the explicit `state.clear_connecting()` at the bottom of the thread would leave the flag permanently `true`, blocking all subsequent Connect attempts. The guard is safe during unwinding because it only touches an `AtomicBool`.
- **`MinecraftApp::last_language` (R-2):** `MinecraftApp` caches `last_language: Language` to deduplicate per-frame `read_config` acquisitions for language synchronisation. The `ui()` method compares `self.last_language` against `self.state.read_config().language` and only calls `i18n::set()` on mismatch. This replaces a per-frame `RwLock::read()` that fired unconditionally even when the language hadn't changed.
- **CJK fonts now appended (R-3):** the system CJK font is *appended* (not prepended) to `egui::FontFamily::Proportional`, so Latin glyphs consistently use the primary font (Ubuntu-Light) and non-Latin glyphs fall through to the CJK font as a last resort. egui 0.34 searches all fonts in the family list, so appending is sufficient for correct CJK rendering.
- **MCP tool radius runtime enforcement (R-4):** `get_nearby_blocks` and `get_nearby_entities` in `tools_query.rs` reject `radius` outside `1..=100` with `BotError::InvalidParams` before dispatching. This matches the `#[schemars(range(min = 1, max = 100))]` JSON Schema contract. Previously, a malicious client could pass `radius=u32::MAX` and trigger pathological O(n) scans or integer-overflow-based incorrect results.
- **`clamp_to_i32` for all u32→i32 conversions (R-5):** `command_validate::clamp_to_i32` clamps a `u32` to `i32::MAX` rather than silently truncating via `as i32` (which wraps to negative for values > `i32::MAX`). All new code that converts `u32` to `i32` must use this helper or `i32::try_from`; a bare `as i32` is forbidden. `handle_walk_direction` additionally uses `saturating_add`/`saturating_mul` for coordinate arithmetic.
- **Container tool validation order (R-6):** `handle_take_from_container` and `handle_put_into_container` validate `count == 0` *before* the `check_container_open` / `is_online` gate, so a malformed parameter always yields `InvalidParams` rather than a misleading state-dependent error. This matches the convention in `tools_item.rs`.
- **`handle_collect_items` naming (R-7):** the local variable and JSON field are named `visited` (not `collected`) because `goto` success only guarantees the bot *reached* the item's position — the server may not have processed the pickup. The result message reads "Visited N item drop location(s); auto-pickup expected on proximity". The entity filter uses `is_collectible_item_entity` and matches ONLY `item` / `item_entity` — `item_frame` and `item_display` are not pickup-able and must not be walked toward. Regression: `test_is_collectible_item_entity_exact_match_only`.
- **`handle_place_block` result message (R-8):** `"slot:N"` internal prefixes are stripped from the result message via `block_type.strip_prefix("slot:").unwrap_or(&block_type)` so the LLM sees a clean block type name rather than an opaque hotbar index.
- **`equip_tool` material preference wiring:** `EquipToolInput.material_preference` is now honoured (previously it was echoed into the response JSON but never affected tool selection). `tools_item::parse_material_tier` maps the string (case-insensitive; `wooden`/`golden` aliases accepted) to a `MaterialTier`; an unknown value returns `InvalidParams`. When present, the MCP handler sends the new `BotCommand::EquipToolWithMaterial(ToolType, MaterialTier)` instead of `EquipTool(ToolType)`; both route to `handle_equip_tool(tool, Option<MaterialTier>)`, which converts the tier to a minimum harvest level via `block_data::harvest_level_of` and passes it to `tool_select::find_tool_in_inventory`. Semantics are **minimum tier** (request Diamond → accept diamond/netherite, reject lower); requesting a tier above every matching tool yields `ToolNotFound`. Note `Gold` shares `Stone`'s harvest level (1), so a gold preference behaves like a stone preference. The plain `EquipTool` variant (used by `CompoundOpExecutor` mining) is unchanged, so its existing call sites did not move — only the four exhaustive `BotCommand` matches (dispatch in `bot/commands.rs`, `validate_command`, and the `require_all_variants`/`count_variants` test guards) gained the new arm.
- **Centralized command validation (dispatch gate):** `CommandExecutor::dispatch` calls `crate::command_validate::validate_command(&cmd)?` as its first statement (before the online check) so every command — including those generated internally by `CompoundOpExecutor` — is bounds-checked in one place. Previously `validate_command` was fully implemented and unit-tested but **never called in production**, so MCP handlers that omitted upper-bound checks (container `slot` > 53 / `count` > 64, `walk_direction` distance > 1000) let out-of-range values reach azalea. Container `count` is now also capped at 64 in `validate_command` to match the schema. Consequence: `DropItem(slot, 0)` is now rejected as `InvalidParams` at the gate (consistent with the MCP layer, which already rejected `count == 0` — see `test_drop_item_count_zero_rejected_at_mcp_layer`); the executor's `handle_drop_item` count==0 noop branch is kept as defensive-only and is no longer reachable via dispatch. Regression tests: `test_dispatch_rejects_container_slot_over_max`, `test_dispatch_rejects_container_count_over_max`, `test_dispatch_rejects_walk_distance_over_max` in `bot/commands.rs::tests`.
- **`INJECTION_READY` removed:** the `AtomicBool` guard flag in `bot/events.rs` was set on connect (`connection.rs`) and cleared on disconnect but never `load()`ed — `BotState::default` never consulted it (each injected static already has an independent fallback), so its doc comment was false. It and the now-unused `AtomicBool` import were deleted. The four injected globals remain `Mutex<Option<T>>` reset per-connection as before.
- **`find_obstacle_block` interpolation (SmartMove):** the informational obstacle scan in `bot/commands.rs` interpolates both axes proportionally (`current + total_d * i / steps`) along the true `current → target` line, instead of the previous `signum()`-based 45° diagonal that overshot the shorter axis and skipped intermediate cells. This only affects the diagnostic `obstacle` field returned by SmartMove, not pathfinding. Regression test: `test_find_obstacle_block_interpolates_line`.
- **`SelfPlayer::position_precise` and `yaw` (W-1):** `SelfPlayer` in `types.rs` carries two `#[serde(skip)]` fields — `position_precise: Option<[f64; 3]>` (sub-block-precision player position, populated by `SnapshotUpdater` from `azalea::entity::Position`) and `yaw: Option<f32>` (horizontal look direction, populated from `azalea::entity::LookDirection::y_rot()`). The top-down renderer (`render_topdown_enhanced`) uses `position_precise` for sub-pixel centre placement (eliminating the up-to-1-block truncation bias) and `yaw` for drawing a heading arrow at the player's pixel. Both are `#[serde(skip)]` so the existing JSON contract for `SelfPlayer` is unchanged — they are derived-internal fields that are `None` when the bot is offline or in test defaults.
- **World view cache (W-2):** `SharedState` has a `last_world_view: Mutex<Option<WorldViewCache>>` for caching the most recent `get_world_view` render. The cache key is `(snapshot_seq, radius, scale)` — `WorldSnapshot::snapshot_seq` is a `#[serde(skip)]` monotonic revision bumped by `SharedState::update_snapshot` / `modify_snapshot`, because the seconds-granularity `timestamp` can repeat for two consecutive 500 ms builds and previously returned a stale PNG. A cache hit returns the same PNG + annotation without invoking `render_topdown_enhanced`. `get_world_view_cache()` / `set_world_view_cache()` / `clear_world_view_cache()` are the accessors. The UI preview panel reads from this cache; the MCP `get_world_view` tool writes to it on cache miss. Cache is single-entry (overwritten on each fresh render), keeping memory bounded. Regression: `test_get_world_view_cache_invalidates_on_snapshot_seq_change`.
- **Section-level chunk scanning (W-3):** `build_snapshot_inner` in `snapshot_updater.rs` now scans dirty chunks section by section (16×16×16 per section, 24 sections per chunk) instead of the previous flat 3× for loop. Before scanning each section, the chunk's `sections[].block_count` is read: if it is `0` (entirely air), the section's 4096 `get_block_state` calls are skipped. For typical surface chunks this skips ~23/24 sections, reducing the per-chunk scan from 98304 calls to ~4096 (only the surface section). The chunk read lock is held briefly to build `sections_to_scan: Vec<usize>`, then released before the per-block scan (which acquires its own lock through `world_guard.chunks.get(&chunk_pos)`).
- **Snapshot retention pruning (W-6):** `build_snapshot_inner` prunes old and newly-read blocks outside `max(chunk_scan_radius, 8)` chunks of the player chunk via `block_within_chunk_radius`. Without this the per-tick `old_snapshot.blocks.clone()` + `block_index` rebuild grew without bound as the bot walked (every chunk ever seen stayed in the snapshot forever). The bound keeps `get_nearby_blocks` (max radius 100 blocks ≈ 7 chunks) fully served. Regression: `test_block_within_chunk_radius_prunes_far_chunks`.
- **`get_world_view` multi-content response (W-4):** the MCP `get_world_view` tool returns `Vec<rmcp::model::Content>` (`[image, text]`) instead of a single `Content::Image`. The text annotation is a JSON object with `center`, `radius`, `scale`, `yaw`, `snapshot_timestamp`, `image_size`, `block_count`, `entity_count`. This required an `impl IntoContents for Vec<Content>` in `patches/rmcp/src/model/content.rs` so the `#[tool]` macro accepts `Vec<Content>` return types. Scale is clamped to `[1,2,4,8]` with a fallback to 1 for invalid values.
- **UI preview panel (W-5):** `src/ui/preview.rs` renders the cached world view PNG as an egui texture. A "Refresh" button clears the cache and calls `get_world_view(state, 8, 2)` to force a re-render. The texture is only rebuilt when the cached annotation JSON changes, avoiding per-frame PNG decode. The panel is registered in `lib.rs` and rendered as a collapsing section after the Status panel. i18n keys `Preview`, `WorldView`, `Refresh`, `WorldViewPlaceholder` have been added to `en.rs` and `zh_cn.rs`.
- **`use_item_on_block` placement verification (R-9):** `BotCommand::UseItemOnBlock(BlockPos, Option<u8>, Option<BlockPos>)` gained a third element — the expected effect cell `T`. The MCP tool takes a `face` argument (up/down/north/south/east/west, default up; parsed by `parse_use_face` in `tools_block.rs`); the effect cell is `T = target + face_offset` and the right-clicked block is `T - Up` (azalea's `block_interact` always reports an Up face — verified in azalea-client 0.15.1 `interact/mod.rs`). With `Some(T)` the executor (a) pre-checks `T` is air/absent and the interaction target is in the snapshot, (b) auto-approaches via `find_standable_neighbor` when farther than 4.5 blocks (survival reach — the server silently rejects out-of-range interactions, the historical fake-success bug), (c) after the click polls `wait_for_block_present` for `snapshot_interval_ms + 250 ms`. Placement items are detected by `item_has_placement_effect` (fluid buckets minus milk, or `BLOCK_TO_TOOL_TYPE` membership); non-placement items skip verification and keep the legacy "Used X" report. A verification timeout returns `BotResult { success: false, ... }` naming the item and the cell — NEVER a fake success. Both the MCP handler and `validate_command` now validate the effect cell `T` itself (e.g. `face=up` at y=320 → effect y=321 is rejected as `InvalidParams` before dispatch). Regression tests: `test_use_item_on_block_face_down_targets_cell_below`, `test_use_item_on_block_face_up_at_build_limit_rejected`, `test_use_item_on_block_invalid_effect_position`, `test_parse_use_face_*` in `tools_block.rs::tests`.
- **`break_block` defaults to the compound flow (R-10):** `BreakBlockInput.use_best_tool` defaults to `true` (`#[serde(default = "default_true")]` returning `Option<bool>`); the default path dispatches `BotCommand::Act(ActAction::Mine, Some(0))` so a bare `break_block` behaves exactly like `act(Mine)` — approach, tool selection (clear `ToolNotFound` alternatives), mining, verification — and the returned `ActResult` carries `action_result` / `reason` / `self_info.position`. `use_best_tool=false` keeps the raw single-packet `BreakBlock`.
- **`get_nearby_blocks` response shape (R-11):** the response is now an OBJECT `{blocks, count, total_matched, truncated, top_only}` (breaking wire change). `NearbyBlocksInput.top_only` (default false) keeps only the highest non-air block per (x, z) column — implemented via sort by `(x, z, Reverse(y))` + `dedup_by` (dedup keeps the FIRST of each run, so highest-y must sort first); `max_blocks` (default 500) caps the payload with an honest `truncated` flag. Runtime now also rejects `max_blocks` outside `1..=10000` (`InvalidParams`) instead of trusting the schema. Always pass a small radius / `top_only=true` / `filter_type` to keep responses small.
- **Movement results carry `position` (R-12):** every tool that can move the bot reports the end position in its result: `handle_collect_items` → `{visited, position}`, `handle_attack_entity` → `{position}` (auto-approach may move the bot), `break_block` default path via `ActResult.self_info`. `move_to`/`smart_move`/`fly_to` already carried `position`. **Round-3: `walk_direction` now carries `data.position` too** — read from `BotActions::position()` (live, zero-wait) after the goto with snapshot fallback, so the throttled snapshot cannot report a pre-move coordinate. Regressions: `test_walk_direction_reports_live_end_position`, `test_walk_direction_reports_snapshot_end_position`.
- **Idle snapshot relaxation (R-13):** `SharedState` tracks `last_command_at` (epoch ms, `mark_command_activity()` called from `CommandExecutor::dispatch`). `events::effective_snapshot_interval_ms` returns the configured `snapshot_interval_ms` while a command arrived within the last 3 s, otherwise `max(configured, 5000)` — a parked bot stops paying for 500 ms rebuilds. Force-refresh requests bypass the throttle gate entirely.
- **Per-command timeouts (R-14):** `AppConfig.fly_timeout_secs` (default 60) gives `fly_to` its own envelope: `channel::BotCommandSender::timeout_for` uses `max(command, fly)` for `FlyTo`, and `handle_fly_to` passes the fly timeout into `goto_with_margin_with_timeout` (new optional-timeout twin of `goto_with_margin`) so the executor always replies before the envelope.
- **`FlyTo` creative gate in the executor (R-17):** `CommandExecutor::handle_fly_to` now returns `BotError::PermissionDenied("FlyTo requires Creative mode")` for non-creative bots instead of `Ok(success:true, reached:false)`. This is defense-in-depth for `Act(Fly)`, which reaches the executor without the MCP `fly_to` handler's creative pre-check. Regression: `test_fly_to_not_creative_is_permission_denied`.
- **Help mode never starts the MCP server (R-15):** the "Minecraft MCP server starting" log moved AFTER `resolve_mode` in `main.rs`; a bare `minecraft-mcp-rs` invocation prints (coloured) help and exits 0 without claiming or starting a server.
- **MCP `instructions` advertise MC support (R-16):** `McpBotServer::get_info` (server.rs) declares "Supported Minecraft version: Java Edition 1.21.11" in `ServerInfo.instructions` so LLM clients see the compatibility contract at `initialize` time.
- **Environment-variable configuration (S-8):** `AppConfig` is loaded **exclusively from `MINECRAFT_MCP_*` environment variables** via `AppConfig::from_env()` (12-factor, cargo-style — **no config file exists anymore**): each field starts from its default and is overridden when the variable is present; malformed values `warn!` and keep the default (startup never fails). `load_from_disk` / `save_to_disk` / `config_path` and the `dirs` dependency were REMOVED. `MINECRAFT_MCP_TOKEN` is the only way to pin the bearer token (unset → fresh random UUID per process start). Runtime changes (UI panel, `update_settings`) apply to the running process only. Env tests live in `src/config.rs::tests` and are serialised via a static `ENV_LOCK` (edition 2024 `std::env::set_var` is `unsafe`; an `EnvGuard` restores prior values).
- **Env semantic validation (S-9):** `AppConfig::from_env` now uses `env_parse_or_validated` for every numeric field so values that parse but are semantically invalid (`0` for positive durations/ports, out-of-range radii) warn and fall back to the default per-field. `main.rs` then runs `config.validate()` as a final gate and falls back to full defaults if anything still slips through. This prevents `MINECRAFT_MCP_SNAPSHOT_INTERVAL_MS=0` (per-tick snapshots) or `MINECRAFT_MCP_COMMAND_TIMEOUT_SECS=0` (instant timeouts) from wedging the runtime. Regression: `test_from_env_zero_values_fall_back_without_panic`.
- **`update_settings` side-effect ordering (2026-08 report P3 round):** `update_settings` must (1) build the candidate, (2) run `candidate.validate()`, (3) commit via `state.update_config(|cfg| *cfg = candidate.clone())`, then (4) call `i18n::set` for a changed language. The P3 refactor briefly lost step (3): the tool replied with the `applied` fields while the live config stayed unchanged, so no settings actually took effect. The existing `test_update_settings_*` suite pins the full path.
- **MCP Config client-URL wildcard rewrite (2026-08 report P3 round):** `ui::mcp_config::format_host_for_url` rewrites bind-all addresses to loopback for the generated client URL: `0.0.0.0` -> `127.0.0.1`, `::` -> `[::1]` (the bind-all form is not connectable by a local MCP client). `test_format_host_ipv4_no_brackets` pins the rewrite.

## 2026-08-22 quality-review fix round (F-series)

- **Compound-op envelopes (F-1):** `BotCommandSender::timeout_for` gives `Act(Mine)` and `Act(CollectItems)` the longer `max(command, fly)` envelope (like `Act(Fly)`). `CompoundOpExecutor::execute_mine_block_with_budget` receives the long-command budget from `handle_act` and returns a structured `success:false` result BEFORE dispatching `BreakBlock` when the mine sleep + verification budget cannot fit; the pre-existing 3-arg `execute_mine_block` remains a `#[cfg(test)]` wrapper. `collect_target_budget` caps each collect-target share by the remaining total and returns `None` (stop + honest partial) when the 2 s floor cannot fit, instead of rounding up and overshooting the envelope.
- **`send_chat` is not an implicit command channel (F-2):** `handle_send_chat` rejects `/`-prefixed messages with `InvalidParams` and redirects callers to `execute_command`. azalea forwards `/x` as a server command packet, which would bypass `execute_command`'s rejection-feedback detection and produce a fake success.
- **MCP dispatch/transport coverage (F-3):** `src/mcp/server.rs` tests now snapshot the 41-tool registry (`list_all` names + read/destructive annotations), drive `tools/call` through a real in-memory rmcp duplex transport (happy path, unknown tool, malformed args), and exercise the axum Bearer wrapper's 401/200 response shape with `tower::ServiceExt::oneshot`. `tower` (already in the lock as a transitive dep) was promoted to a dev-dependency.
- **Connection latch fix + UI matrix test (F-4/F-5/F-12):** the cancel-window restart branch now routes through `prepare_cancel_restart` which consumes `session_was_online` and resets both counters (regression-tested). `ui::app` has a parameterized dirty→apply→read_config round-trip for every `EditConfig` field.
- **Mining model (F-7/F-20):** wrong-tool and under-harvest-level breaking use the vanilla 100-tick branch (`hardness * 5 / speed`, stone by hand = 7.5 s), replacing the inflated 11.25 s formula; `calculate_mine_time` now accounts for the tool material's harvest level (wood pickaxe on iron ore = 15 s non-harvest breaking). Assertions use a 1e-9 tolerance instead of `f64::EPSILON`.
- **Config/snapshot hardening (F-6/F-8/F-9/F-11/F-22/F-23/F-26/F-31):** `modify_snapshot` rebuilds `block_index`; `from_env` reconciles reconnect max ≥ initial and `main.rs` re-applies `--stdio` after the default fallback; `validate` rejects HTTP + non-loopback + auth-disabled and validates `mc_address` as IP/hostname; chunk section bases derive from `ChunkStorage::min_y`; overflow-chunk dirty blocks are not evicted before their deferred scan; failed snapshot builds retry after 250 ms; chat/command lines are capped at 256 chars.
- **Honest failure / not-found semantics (F-27/F-33/F-34):** entities missing from the ECS index use `UNKNOWN_ENTITY_ID` (u32::MAX) instead of 0; missing entity IDs map to `BotError::EntityNotFound` (`RESOURCE_NOT_FOUND`, reason `entity_not_found`) in both MCP and executor layers; a verified `place_block` success never reports "(empty slot)" when the throttled snapshot has not caught up.
- **Test hygiene (F-15/F-16/F-17/F-18/F-37):** the goto-notify test now actually awaits the notified future; proptest tautologies were replaced with round-trip / exact-bounds / no-INFINITY-escape properties; duplicated inline throttle tests in `events.rs` were removed; `I18N_TEST_LOCK` serialises process-wide language mutations; compound state/event variant tests use exhaustive compile-time matches; the atomic-toggle test asserts the real final value.
- **Doc/dependency drift (F-24/F-28/F-29/F-32/F-35):** `cli.rs` doc sentence completed; duplicated/truncated `plan_dirty_chunk_scan` docs fixed; `ConnectionManager::disconnect` renamed `simulate_offline_for_tests` and `#[cfg(test)]`; `server.rs` says 41 tools / rmcp 1.8.0; dead `src/mcp/mod.rs` removed; `IntoCallToolResult` documents the deliberate error-over-isError contract.
